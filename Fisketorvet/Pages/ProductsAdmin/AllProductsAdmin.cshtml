@page
@model Fisketorvet.Pages.Products.AllProductsModel
@using Fisketorvet.Interfaces
@inject IProductRepository repository
@{
    ViewData["Title"] = "AllProductsAdmin";

}

<h1>List of all Products</h1>

<p>
    <a asp-page="CreateProduct"><button type="button" class="btn btn-outline-warning">Create New Product</button></a>
</p>

<div>
    <ul>
        <li> There are @repository.AllProducts().Count products</li>
    </ul>
</div>

<form>
    <p>
        Search: <input type="text" asp-for="FilterCriteria" />
        <input type="submit" value="Filter" />
    </p>
</form>

@*<table class="table">
    <thead>
        <tr>
            <th> Name </th>
            <th> Store </th>
            <th> Price </th>
            <th> Image </th>
        </tr>
    </thead>*@
<table class="table table-striped table-dark">
    <thead>
    <tr>
        <th scope="col">Name</th>
        <th scope="col">Store</th>
        <th scope="col">Price</th>
        <th scope="col">Image</th>
    </tr>
    </thead>
    <tbody>
        @foreach (var p in Model.Products)
        {
        <tr>
            <td>@p.ProductName</td>
            <td>@p.ProductName</td>
            <td>@p.Price</td>
            <td>
                <img src="~/Images/ProductImages/@p.ImageName" width="90" height="130">
            </td>
            <td>                
                <a asp-page="DeleteProduct" asp-route-id="@p.ProductId"><button type="button" class="btn btn-outline-danger">Delete</button></a>              
            </td>
        </tr>
        }
    </tbody>
</table>
